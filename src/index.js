import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; //First example

ReactDOM.render(
  <App title={<h1 className="title">GITHUB CARDS</h1>} />,
  document.getElementById("root")
);
